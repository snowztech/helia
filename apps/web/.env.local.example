# apps/web only reads one variable. The default points at the local API
# from `make dev`, so most people don't need this file at all.
#
# Copy to apps/web/.env.local only if your API runs somewhere else.

# Base URL of the Helia API. Must start with http:// or https://.
NEXT_PUBLIC_API_URL=http://localhost:4000
